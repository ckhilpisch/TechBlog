<h1 class="text-center" id="home-welcome"><span>Welcome to the Tech Blog!</span></h1>
<br>
<br>
<h2 class="text-center"><span>**Enjoy the technology insights**</span></h2>

<div class="row">
  <div class="col">
    {{#each blogs as |blog| }}
    <div class="card text-center">
      <div class="card-header">
        <h2 class="card-title"><a href="/blog/{{blog.id}}">{{blog.name}}</a></h2>
      </div>
      <div class="card-body">
        <p>Created by: {{blog.user.name}}</p>
        <p class="card-text">{{blog.content}}</p>
      </div>
      <div class="card-footer text-muted">
        Created On: {{format_date blog.date_created}}
      </div>
    </div>
    {{/each }}
  </div>
</div>